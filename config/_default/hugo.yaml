title: "Hugo shortcodes and render hooks"
disableKinds: ["rss", "taxonomy", "term"]

# Do not publish content in "_includes" directories as pages
cascade:
  build: { list: never, publishResources: false, render: never }
  target: { path: "{,/**}/_includes{,/**}" }
